spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/miaosha?serverTimezone=UTC
  thymeleaf:
    cache: false
  cache:
    type: redis

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# tomcat 工作线程池最大设置为 1
server:
  tomcat:
    max-threads: 200
  port: 20013
  servlet:
    context-path: /lmc
    session:
      cookie:
        name: lmc

custom:
  fyaj:
    url: http://2.208.64.254:8090/channel/gG2foEk5lz/services/FgwServices?wsdl

clientLog: /home/enlink/clientlog